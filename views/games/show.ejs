<section id="main" class="container">
    <div class="row">
        <div class="col-12">
            <section class="box">
                <div class="table-wrapper">
                    <table>
                        <thead>
                            <tr>
                                <td>
                                    <b>Question</b>
                                </td>
                                <td>
                                    <b>Answer</b>
                                </td>
                                <td><b>Result</b></td>
                            </tr>
                        </thead>
                        <% for (step of game.gameSteps) { %>
                        <% if (step.userId !== session.user.id) { continue;}%>
                        <tr>
                            <td>
                                <%= step.quiz.question %>
                            </td>

                            <% include _result.ejs %>
                        <% } %>
                    </table>
                     
                </div>
                
            </section>
        </div>
    </div>
</section>
