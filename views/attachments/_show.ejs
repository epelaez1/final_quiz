
<% if (attachment) { %>

    <a href='<%= attachment.url %>'
       type='<%= attachment.mime %>'
       target='_blank'>

        <% if ( attachment.mime === "image/jpeg" ||
                attachment.mime === "image/gif" ||
                attachment.mime === "image/jpg" ||
                attachment.mime === "image/png" ||
                attachment.mime === "image/gif" ) { %>

        <%- cloudinary.image(attachment.public_id, {width: 150, secure: true}) %>

        <% } else {  %>

        <img src='/images/none.png' width="150px"/>

        <% } %>
    </a>
<% } else { %>
    <img src="/images/none.png" width="50px" />
<% } %>
